# Original Scripts

Place the keyboard/mouse automation script here before splitting it into reusable control functions.

The extension currently uses a Windows PowerShell control adapter. When an existing script is added, keep the original copy here, then move reusable operations into `.pi/extensions/computer-control/src/control.ts` or a dedicated adapter module.

